# Stripe — copia este archivo a .env.local y pon tus claves reales
# Stripe — copy this file to .env.local and add your real keys
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxx
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Opcional: si creas Prices en el dashboard de Stripe, pega sus IDs aquí.
# Si se dejan vacíos, el checkout crea el precio al vuelo con price_data.
STRIPE_PRICE_BASIC=
STRIPE_PRICE_BEAST=
STRIPE_PRICE_ELITE=
